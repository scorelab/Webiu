import React from "react"
import PropTypes from "prop-types"
import "./style.sass"

const ListPagination = ({items}) => {
  return (

  )
}

ListPagination.propTypes = {
  items: PropTypes.array,
}

export default ListPagination